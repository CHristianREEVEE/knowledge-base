# 每日记忆 2026-09-07

> Zero 产出 ｜ 当日对话话题索引+结论。

## 工作台补时即时入账上线
- 触发场景：TA 没开计时器手动 +60 分钟，底部统计纹丝不动——旧版 idle 补时只挂内存变量（timerAdjustSec），不落 localStorage、不入 timer_segments，刷新即丢
- 改造：未计时状态 +按钮直写 manual 段入账落盘，统计即时刷新；新增「+1时」按钮（TA 高频场景"估摸 1 小时"一键入账）；连续补点 10 分钟内合并成段；−按钮从最后一段回退
- 验证链：node --check → 本地浏览器实测（入账/合并/持久/回退）→ 线上 md5 字节核对 → CDN 切换确认

## 自用工具 UI 零解释（TA 新铁律）
- 补时明细只显示「加时」两个字——不编时间窗、不加图标："反正我自己用，没必要给自己做解释"
- 给自用工具加解释性 UI 是白费功夫，用户就是作者本人

## 仓库架构问答
- 云同步版与本地版不是两个仓库：GitHub workbench = 本地版唯一正本（纯 localStorage）；云版在服务器 /opt/sites/workbench（多一层同步代码+import.html）；/opt/sites/wbrepo = GitHub 副本存档
- TA 明确**不要云同步**：怕不稳定，坚持纯本地版——云版转维护模式，改动需手动移植

## 其他
- 世界备份例行全绿；09-04 起存档 md5 全同 = 世界休眠（TA 停外部挂机脚本后无 agent 活动），属预期不报警
